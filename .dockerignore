**/node_modules
.next
.git
.claude
scripts
tests
specs
*.md
.env*
livekit-dev.yaml
docker-compose-dev.yml
# The Next.js app image never reads the agents workspace (it has its own
# build contexts under agents/) — keep it out of the app build context.
agents
# Local data/secrets that must never enter an image context
secrets
supabase/docker
